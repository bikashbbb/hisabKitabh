// PLANNING : create a class that holds all the function related to hive 

import 'package:app/screens/dataentry/model/datamodel.dart';

class HiveDatabase{
  /// saves the transaction model into the hive databasee.
  bool saveModel(Transaction object){
    /* if(){
      return true;
    } */
    return false;
  }
  
  static int giveUniqueId(bool isAdddaily){
    return 1;
  }

}